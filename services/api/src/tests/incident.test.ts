import { describe, test, expect } from "@jest/globals";

describe("test incident endpoints", () => {
  describe("POST /incident", () => {
    test("should trigger the incident flow", async () => {
      // await request(app).post(`/usage`).send().expect(200);
      expect(true).toBe(true);
    });
  });
});
